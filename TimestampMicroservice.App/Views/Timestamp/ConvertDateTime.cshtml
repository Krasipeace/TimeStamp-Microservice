@model ConvertDateTimeViewModel
@{
    ViewData["Title"] = ConvertDateAndTimeToTimestampTitle;
}

<h2 class="text-center">Get Date and Time from Timestamp</h2>
<div class="row">
    <div class="text-center">
        <form asp-action="ConvertDateTime" asp-controller="Timestamp" method="post">
            <div class="form-group align-content-center">
                <label asp-for="DateAndTime" class="control-label">Date and Time:</label>
                <div class="align-content-center">
                    <input asp-for="DateAndTime" class="form-control text-center" id="utc" />
                </div>
                <span asp-validation-for="DateAndTime" class="text-danger"></span>
            </div>
            <br />
            <button type="submit" class="btn btn-outline-success">Get Unix Time</button>
        </form>
        <br />
        @if (Model != null)
        {
            <div class="row">
                <div class="text-center">
                    <h4 class="text-center">Timestamp:</h4>
                    <p class="text-center">@Model.EpochTime</p>
                </div>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script src="~/js/site.js"></script>
}